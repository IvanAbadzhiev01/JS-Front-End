function SumFirstAndLastElement(arr){
    let firstElement = arr[0];
    let lastElement = arr[arr.length-1];

    console.log(firstElement + lastElement);
}

SumFirstAndLastElement([10, 11 , 13 , 100 , 5]);